{% import "./chartMacro.njk" as chart with context %}

<script>

const cumulativeCases = {{ cumulativeCases | dump(4) | safe }};

</script>

{{ chart.create(
	'cumulativeCasesChart',
	'Total Cumulative Cases',
	'cumulativeCases'
) }}
